kind: Service
apiVersion: v1
metadata:
  name: nginx-cip
spec:
  selector:
    project: expense
    module: frontend
    component: frontend
    environment: dev
  ports:
  - protocol: TCP
    port: 80 #service port
    targetPort: 80
